import React from 'react'
import { storiesOf } from '@storybook/react'
import ScoreBoard from '../src/components/ScoreBoard'

storiesOf('ScoreBoard', module).add('default', () => <ScoreBoard></ScoreBoard>)
